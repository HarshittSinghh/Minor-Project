<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Markify</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: Arial, sans-serif;
    }

    body {
      background-color: #f8f9fa;
      color: #333;
    }

    header {
      background-color: #002147;
      color: #FFD700;
      text-align: center;
      padding: 40px 20px;
      border-bottom: 4px solid #004080;
      height: 15rem;
    }

    header h1 {
      font-size: 2.8rem;
      margin-bottom: 10px;
    }

    header p {
      font-size: 1.2rem;
      font-weight: 300;
    }

    .navbar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      background-color: #002147;
      color: white;
      padding: 15px 20px;
    }

    .navbar .logo {
      display: flex;
      align-items: center;
    }

    .navbar .logo img {
      width: 40px;
      margin-right: 10px;
    }

    .navbar .logo span {
      font-size: 1.6rem;
      font-weight: bold;
    }

    .navbar .nav-links {
      display: flex;
      gap: 20px;
    }

    .navbar .nav-links a {
      color: #FFD700;
      text-decoration: none;
      font-size: 1rem;
      transition: color 0.3s;
    }

    .navbar .nav-links a:hover {
      color: white;
    }

    .navbar .hamburger {
      display: none;
      font-size: 1.5rem;
      cursor: pointer;
    }

    @media (max-width: 768px) {
      .navbar .nav-links {
        display: flex;
        flex-direction: column;
        background-color: #111;
        position: absolute;
        top: 0;
        right: 0;
        width: 100%;
        text-align: center;
        gap: 10px;
        z-index: 10;
        padding: 100px 0;
        opacity: 0;
        transform: translateY(-100%);
        transition: transform 0.5s ease, opacity 0.5s ease;
      }
      .navbar .nav-links a{
        padding: 20px;
        background-color: #002147;
      }
      .navbar .nav-links.active {
        transform: translateY(0);
        opacity: 0.95;
      }

      .navbar .hamburger {
        display: block;
      }
    }

    main {
      padding: 40px 20px;
      text-align: center;
    }

    .card-container {
      display: flex;
      justify-content: center;
      gap: 30px;
      margin-top: 20px;
      flex-wrap: wrap;
    }

    .card {
      background-color: white;
      border: 2px solid #002147;
      border-radius: 10px;
      width: 300px;
      padding: 20px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      text-align: center;
      transition: transform 0.3s, box-shadow 0.3s;
    }

    .card:hover {
      transform: translateY(-5px);
      box-shadow: 0 6px 15px rgba(0, 0, 0, 0.15);
    }

    .card h2 {
      font-size: 1.6rem;
      margin-bottom: 15px;
      color: #002147;
    }

    .card p {
      font-size: 1rem;
      margin-bottom: 20px;
      color: #555;
    }

    .btn {
      display: inline-block;
      background-color: #FFD700;
      color: #002147;
      padding: 12px 25px;
      text-decoration: none;
      border-radius: 5px;
      font-size: 1rem;
      font-weight: bold;
      transition: background-color 0.3s, transform 0.3s;
    }

    .btn:hover {
      background-color: #ffc107;
      transform: translateY(-2px);
    }

    footer {
      margin-top: 30px;
      background-color: #002147;
      color: #FFD700;
      text-align: center;
      padding: 15px 0;
      font-size: 0.9rem;
    }

    footer a {
      color: #FFD700;
      text-decoration: none;
    }

    footer a:hover {
      text-decoration: underline;
    }
  </style>
</head>

<body>
  <!-- Navbar -->
  <nav class="navbar">
    <div class="logo">
      <!-- <img src="logo.png" alt="Logo"> -->
      <span style="color:#ffc107;">Markify</span>
    </div>
    <div class="nav-links">
      <a href="/">Home</a>
      <a href="about.html">About Us</a>
    </div>
    <div class="hamburger" onclick="toggleMenu()">&#9776;</div>
  </nav>

  <!-- Header -->
  <header>
    <div class="container">
      <h1>Welcome to the Portal</h1>
      <p>Select your role to proceed:</p>
    </div>
  </header>

  <!-- Main Content -->
  <main>
    <div class="card-container">
      <div class="card">
        <h2>Admin Login</h2>
        <p>Manage portal settings and user accounts.</p>
        <a href="/admin" class="btn">Login as Admin</a>
      </div>
      <div class="card">
        <h2>Teacher Login</h2>
        <p>Access teaching tools and student data.</p>
        <a href="/teacher" class="btn">Login as Teacher</a>
      </div>
      <div class="card">
        <h2>Student Dashboard</h2>
        <p>View your dashboard and learning resources.</p>
        <a href="/dashboard" class="btn">Go to Dashboard</a>
      </div>
    </div>
  </main>

  <!-- Footer -->
  <footer>
    <p>&copy; 2025 Markify. All Rights Reserved.</a></p>
  </footer>

  <script>
    function toggleMenu() {
      const navLinks = document.querySelector('.nav-links');
      navLinks.classList.toggle('active');
    }
  </script>
</body>

</html>